name: Build Freeze Daemon

on:
  push:
    branches: [ main ]
    tags:     [ 'v*' ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. 拉取代码
      - name: Checkout
        uses: actions/checkout@v4

      # 2. 安装 Android NDK
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install NDK r27
        run: |
          echo "y" | sdkmanager "ndk;27.2.12479018"
          echo "NDK_HOME=$ANDROID_SDK_ROOT/ndk/27.2.12479018" >> $GITHUB_ENV

      # 3. 安装宿主机工具链
      - name: Install host toolchain
        run: |
          sudo apt-get update -q
          sudo apt-get install -y \
            clang llvm \
            libelf-dev zlib1g-dev \
            libbpf-dev \
            linux-headers-$(uname -r) \
            bpftool

      # 4. 编译 eBPF 字节码（修复 vmlinux.h 问题）
      - name: Compile eBPF program
        run: |
          # 生成 vmlinux.h（最可靠方式）
          if [ -f /sys/kernel/btf/vmlinux ]; then
            bpftool btf dump file /sys/kernel/btf/vmlinux format c > /tmp/vmlinux.h
            echo "Generated vmlinux.h from host BTF"
          else
            echo "Warning: Using fallback vmlinux.h"
            wget -q https://raw.githubusercontent.com/libbpf/libbpf/master/include/uapi/linux/bpf.h -O /tmp/vmlinux.h
          fi

          clang -target bpf -O2 -g \
            -I /tmp \
            -I src \
            -c src/platform/freeze_monitor.bpf.c \
            -o freeze_monitor.bpf.o
          
          echo "eBPF object: $(ls -lh freeze_monitor.bpf.o)"

      # 5. 为 arm64 交叉编译 libbpf 静态库
      - name: Cross-compile libbpf for arm64
        run: |
          NDK=$NDK_HOME
          TOOLCHAIN=$NDK/toolchains/llvm/prebuilt/linux-x86_64
          SYSROOT=$TOOLCHAIN/sysroot
          CC=$TOOLCHAIN/bin/aarch64-linux-android33-clang

          git clone --depth=1 --branch v1.4.0 https://github.com/libbpf/libbpf.git /tmp/libbpf
          cd /tmp/libbpf/src
          make CC="$CC" \
               CFLAGS="--sysroot=$SYSROOT -target aarch64-linux-android33" \
               OBJDIR=/tmp/libbpf-build \
               BUILD_STATIC_ONLY=1 \
               -j$(nproc)

          mkdir -p $GITHUB_WORKSPACE/prebuilt/libbpf/include
          cp /tmp/libbpf-build/libbpf.a  $GITHUB_WORKSPACE/prebuilt/libbpf/
          cp -r /tmp/libbpf/src/*.h       $GITHUB_WORKSPACE/prebuilt/libbpf/include/
          cp -r /tmp/libbpf/src/uapi      $GITHUB_WORKSPACE/prebuilt/libbpf/include/
          echo "libbpf.a: $(ls -lh $GITHUB_WORKSPACE/prebuilt/libbpf/libbpf.a)"

      # 6. 编译 C++ Daemon
      - name: Compile Daemon (arm64)
        run: |
          cmake -B build \
            -DCMAKE_TOOLCHAIN_FILE=$NDK_HOME/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-33 \
            -DANDROID_STL=c++_static \
            -DCMAKE_BUILD_TYPE=Release
          cmake --build build --target freeze_daemon -j$(nproc)
          echo "Binary: $(ls -lh build/freeze_daemon)"

      # 7. 组装 KernelSU 模块包
      - name: Package module
        run: |
          PKG=module_package
          mkdir -p $PKG/{bin,config,webui,logs}

          cp module.prop                  $PKG/
          cp scripts/service.sh           $PKG/service.sh
          chmod +x $PKG/service.sh

          cp build/freeze_daemon          $PKG/bin/
          cp freeze_monitor.bpf.o         $PKG/bin/
          chmod +x $PKG/bin/freeze_daemon

          cp config/freeze.conf           $PKG/config/
          cp config/user_whitelist.txt    $PKG/config/
          cp config/system_blacklist.txt  $PKG/config/

          cp webui/index.html             $PKG/webui/

          cd $PKG
          zip -r ../freeze-daemon-${{ github.ref_name }}.zip .
          cd ..
          echo "Package: \( (ls -lh freeze-daemon- \){{ github.ref_name }}.zip)"

      # 8. 上传 Artifact
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: freeze-daemon-module
          path: freeze-daemon-*.zip
          retention-days: 30

      # 9. 发布 Release
      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: freeze-daemon-*.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}